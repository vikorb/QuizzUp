{
  "name": "quizzup",
  "version": "1.0.0",
  "private": true,
  "description": "QuizzUp monorepo root — git hooks (husky + commitlint) partagés entre les apps",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.4.2",
    "@commitlint/config-conventional": "^20.4.2",
    "husky": "^9.1.7"
  }
}
